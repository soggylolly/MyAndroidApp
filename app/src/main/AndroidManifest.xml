<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions needed for Firebase, location reminders and notifications -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAndroidApp">

        <!-- MainActivity is used to create a new task.
         android:exported="false" means this activity can only be opened
         from inside the application. -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Screen used for picking a location on the map -->
        <activity
            android:name=".LocationPickerActivity"
            android:exported="false" />

        <!-- ToDoListActivity displays the list of tasks.
        It is marked as the launcher activity, meaning it is the first screen shown
        when the application starts. -->
        <activity
            android:name=".ToDoListActivity"
            android:exported="true">

            <!-- Intent filter defines how the activity is launched -->
            <intent-filter>
                <!-- Marks this activity as the main entry point -->
                <action android:name="android.intent.action.MAIN" />

                <!-- Makes the app appear in the device launcher -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- FileProvider is required to securely share files between the application
         and other apps such as the device camera.
         It is used when capturing images for tasks. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <!-- Links the provider to the file_paths.xml configuration
             which defines where files can be shared from. -->
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>

</manifest>
