<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main vertical form for adding or editing a reminder -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Page title changes to Edit Reminder when editing -->
        <TextView
            android:id="@+id/formTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add Reminder"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- User types the reminder title here -->
        <EditText
            android:id="@+id/taskTitleView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Title" />

        <!-- User types a longer description here -->
        <EditText
            android:id="@+id/taskDescriptionView"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:gravity="top"
            android:hint="Description" />

        <!-- Date box opens DatePickerDialog from Java -->
        <EditText
            android:id="@+id/taskDueDateView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:focusable="false"
            android:hint="Due Date"
            android:onClick="openDatePicker" />

        <!-- Time box opens TimePickerDialog from Java -->
        <EditText
            android:id="@+id/taskDueTimeView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:focusable="false"
            android:hint="Due Time"
            android:onClick="openTimePicker" />

        <!-- Opens the phone camera -->
        <Button
            android:id="@+id/imageButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="onCameraClick"
            android:text="Take Photo" />

        <!-- Shows the photo after the camera returns -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:background="#DDDDDD"
            android:scaleType="fitCenter" />

        <!-- Saves the current GPS or network location -->
        <Button
            android:id="@+id/locationButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="saveCurrentLocation"
            android:text="Use Current Location" />

        <!-- Opens the simple map picker screen -->
        <Button
            android:id="@+id/mapButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="openMap"
            android:text="Choose Location On Map" />

        <!-- Shows the saved latitude and longitude -->
        <TextView
            android:id="@+id/locationText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="No location saved" />

        <!-- Saves the reminder to Room database -->
        <Button
            android:id="@+id/saveTaskButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Save Reminder" />

    </LinearLayout>
</ScrollView>
